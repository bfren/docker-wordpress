#!/command/with-contenv bash

set -euo pipefail
export BF_E=`basename ${0}`


#======================================================================================================================
# Call wp-cron executable
#======================================================================================================================

[[ "${WP_USE_SYSTEM_CRON-}" = "1" ]] && wp-cron
