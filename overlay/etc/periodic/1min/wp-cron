#!/bin/withenv sh

set -euo pipefail


#======================================================================================================================
# Call wp-cron executable if system cron is enabled
#======================================================================================================================

bf-env-check WP_USE_SYSTEM_CRON && wp-cron
